/*          switch (options) {
             case 'justLinks':
                 resolve(fileWithLinks);
                 break;

         } */


         validate
         
/* function validate (validLinks) {
    return new Promise(function (resolve, reject) {
        const getLinks = validLinks.map(function (link) {
            console.log(validLinks)
        })
    })        
} */